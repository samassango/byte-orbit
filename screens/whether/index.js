import React from "react";
import { View } from "react-native";
const WhetherScreen = () => {
  const navigationOptions = {
    title: "Home"
  };

  /* render function, etc */
  return <View> Testing</View>;
};
export default WhetherScreen;
